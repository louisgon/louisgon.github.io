{# Passed Variables #}
{% set classes = classes|default('') %}
{% set data = data|default({}) %}

{# HTML #}
<script {{ htmlAttr({
  class: classes,
  type: 'application/ld+json',
}) }}>{{ base64(data) }}</script>
